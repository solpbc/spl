{
	"name": "spl-mobile",
	"version": "0.1.0",
	"private": true,
	"description": "sol private link — example mobile client (Bun/TypeScript)",
	"license": "AGPL-3.0-only",
	"type": "module",
	"main": "src/index.ts",
	"bin": {
		"spl-mobile": "src/index.ts"
	},
	"scripts": {
		"dev": "bun run src/index.ts",
		"start": "bun run src/index.ts",
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"lint": "biome check src/ tests/",
		"format": "biome format --write src/ tests/"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/node": "^22.0.0",
		"bun-types": "1.3.12",
		"typescript": "^5.5.0"
	}
}
